# GCE SSH key lookup: tries OS Login first, then instance/project metadata.
# Required because the root filesystem is read-only (erofs + dm-verity),
# so google-guest-agent cannot write to ~/.ssh/authorized_keys.
AuthorizedKeysCommand /usr/bin/gce-authorized-keys %u
AuthorizedKeysCommandUser root
